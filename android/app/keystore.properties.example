# Copy this file to keystore.properties (same folder) and fill in real values.
# keystore.properties is gitignored — it never gets committed.
#
# In CI (GitHub Actions), these same values come from repo Secrets instead
# — see .github/workflows/android.yml.

STORE_PASSWORD=
KEY_ALIAS=
KEY_PASSWORD=
ADMOB_AD_UNIT_ID=
